# Recomendado: Python >= 3.11
numpy>=1.24,<3
scipy>=1.10,<2
scikit-learn>=1.3,<2
aiohttp>=3.9,<4
brainflow>=5.10,<6

# Offline / exportação (CSV). Se não for usar offline, pode remover.
pandas>=2.0,<3

# Observação: para modo "dataset" você precisa do pacote local `bciflow/`
# e dos arquivos em data/ssvep/ (não é instalado via pip).
